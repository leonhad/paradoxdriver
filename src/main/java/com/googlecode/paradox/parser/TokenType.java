package com.googlecode.paradox.parser;

/**
 *
 * @author lcosta
 */
public enum TokenType {

    IDENTIFIER,
    SELECT,
    INSERT,
    DELETE,
    UPDATE,
    FROM,
    WHERE,
    JOIN,
    INNER,
    OUTER,
    EQUALS,
    PLUS,
    MINUS,
    LPAREN,
    RPAREN,
    COMMA
}
